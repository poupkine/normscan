import { type FC } from 'react';
import { Container } from '../Container';

export const Header: FC = () => {
  return (
    <header>
      <Container>
        Header
      </Container>
    </header>
  );
};
